{
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "Kotlin hello world for Cloudflare Workers",
  "main": "build/distributions/{{ project-name }}.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "devDependencies": {
  }
}
